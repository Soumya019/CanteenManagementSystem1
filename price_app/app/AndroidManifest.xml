<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gariaelectric.pricelookup"
    android:versionCode="4"
    android:versionName="1.3">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />

    <!-- only used to auto-fetch a product photo when a new item is added
         without one; price lookup itself works fully offline -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- speech recognition is delegated to the Google voice input activity,
         so no RECORD_AUDIO permission is needed by this app itself -->
    <queries>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <application
        android:label="Garia Electric Prices"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddItemActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".PhotoViewActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen" />
    </application>
</manifest>
